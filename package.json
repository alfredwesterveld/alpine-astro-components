{
  "name": "alpine-astro-components",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "bun run --cwd packages/spa-restore build",
    "test": "bun run --cwd packages/spa-restore test"
  }
}
